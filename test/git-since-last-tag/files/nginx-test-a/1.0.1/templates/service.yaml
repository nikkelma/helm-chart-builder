apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-test-a.fullname" . }}
  labels:
    {{- include "nginx-test-a.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-test-a.selectorLabels" . | nindent 4 }}
