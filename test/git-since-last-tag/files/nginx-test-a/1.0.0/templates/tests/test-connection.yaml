apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-test-a.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-test-a.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-test-a.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
