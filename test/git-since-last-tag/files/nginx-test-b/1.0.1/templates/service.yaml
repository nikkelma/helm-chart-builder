apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-test-b.fullname" . }}
  labels:
    {{- include "nginx-test-b.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-test-b.selectorLabels" . | nindent 4 }}
